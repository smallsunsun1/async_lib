file(GLOB header_files "*.h")
file(GLOB object_files "*.cpp")
add_library(async_support ${object_files})
set_target_properties(async_support PROPERTIES PUBLIC_HEADER "${header_files}")
target_link_libraries(async_support PUBLIC absl::span)
